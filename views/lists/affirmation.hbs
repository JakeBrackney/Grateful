<h1>My Affirmations</h1>
{{!-- 
<section class="profile">
  <h2>{{user.local.email}}</h2>
  {{#if user.lists}}
    <div class="items">
      {{#each user.lists}}
         {{'./affirmation.hbs'}}
      {{/each}}
    </div>
  {{else}}
    <p>No affirmations yet!</p>
  {{/if}}
</section> --}}